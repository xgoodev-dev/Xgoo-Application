import { View } from 'react-native';

/** Expo template leftover — keep inert so Expo Go never loads worklets. */
export function AnimatedSplashOverlay() {
  return null;
}

export function AnimatedIcon() {
  return <View />;
}
